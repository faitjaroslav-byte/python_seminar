# 2. První program

<div class="lesson-meta">
<strong>Doporučený čas:</strong> 45–60 minut<br>
<strong>Výstup:</strong> Rozumíš funkci `print()`, pořadí příkazů a komentářům.
</div>
## První výstup

```python
print("Ahoj světe!")
```

## Více příkazů

```python
print("První řádek")
print("Druhý řádek")
```

## Komentáře

```python
# Tato zpráva se zobrazí uživateli.
print("Vítej v Pythonu")
```

!!! warning "Python rozlišuje velikost písmen"
    `print()` a `Print()` nejsou stejné názvy.

[Stáhnout ukázku](code/prvni_program.py){ .md-button }
